# 推妖镜 BotZapper · 产品设计文档

> 在 Twitter/X 评论区识别垃圾账号（黄推引流、钓鱼、复读机），高亮标注并一键调用平台原生拉黑的浏览器扩展。

---

## 1. 命名与品牌

| 语言 | 名字 | 作用 |
|------|------|------|
| 中文 | **推妖镜** | 传播名。借"照妖镜"的公共记忆——镜子一照，妖怪现形。推（推特）+ 妖（垃圾账号）+ 镜（识别现形），目标、动作、形象三合一，口口相传零解释成本 |
| 英文 | **BotZapper** | 商店合规名。Bot（机器人）+ Zapper（电蚊拍/消灭器），中性直白，给商店审核看是一个规整的效率工具 |

**命名检验：**
- 借公共记忆：照妖镜是西游记/民间传说中家喻户晓的意象，画面感强；
- SEO 独占："推妖镜"全网无重名，搜索流量直通项目；
- 自带社交谈资："你装个推妖镜照照"本身就是一句可用的话；
- 中英分工：中文带情绪打传播，英文走中性求合规。

**视觉建议：** Logo = 一面古镜 + 镜中妖怪剪影（现形瞬间）。UI 主色可用"朱砂红 + 铜镜金"，与高亮黄框、拉黑红按钮的功能色呼应。

---

## 2. 六条设计原则

这六条是整个产品的宪法，所有功能取舍都回到这里校验：

1. **原生拉黑优先，前端隐藏只是兜底。** 只做 `display:none` 是掩耳盗铃：换设备现原形、账号还在吸血、没有"连根拔掉"的踏实感。必须调用 X 官方原生拉黑接口，服务端生效、全端同步。
2. **半自动，不做静默全自动。** 插件负责"照妖"（识别+高亮），用户负责"收妖"（手动确认拉黑）。全自动高频扫描既踩风控红线，也让用户失去掌控感——要保留亲手开枪的爽感。
3. **防误伤高于召回率。** 宁可漏掉两个，绝不错杀一个正常人。误杀正常用户是治理类工具的死罪。
4. **零服务器成本。** 识别全部在用户浏览器本地完成；拉黑请求用用户自己的会话从本地发出；规则库托管在 GitHub/静态边缘分发。不背月租账单，项目才能一直活下去。
5. **开源是生存前提，不是慈善。** 一个能代替你执行账号操作的插件，闭源就是信任死锁。源码公开是让用户敢装的唯一底牌。
6. **慢即是快：把油门踩稳，把刹车装好。** 拉黑请求串行排队 + 随机延迟 + 限流熔断，宁可后台慢跑半分钟，绝不拿用户主账号冒险。

---

## 3. 功能设计（按优先级分期）

### P0 · MVP（目标：一晚上写出来，自己先用爽）

核心单点动作：**看到黄框 → 点一下 → 世界清静**。以下之外的一切都是修饰。

- **DOM 监听**：MutationObserver 监听时间线与评论列表，新推文/评论实时进入检测队列；
- **识别引擎 v1**：本地关键词规则匹配（引流黑话、钓鱼链接模式、全角字符伪装）；
- **高亮标注**：命中评论加显眼黄框 + "疑似妖物"角标；
- **单条拉黑按钮**：每条黄框内嵌红色"拉黑"按钮，替代原生"三点菜单→拉黑→二次确认"的四步流程；
- **一键清理**：评论区顶部徽章显示"本页捕获 N 个妖怪"，点击批量拉黑全部；
- **拉黑队列调度器**：串行执行、每请求随机延迟 800ms–2s、HTTP 429 立即熔断暂停；
- **已拉黑本地标记**：`chrome.storage.local` 记录已处理账号，刷新页面不重复发请求；
- **登录用户强制白名单**：绝不给自己打框。

### P1 · 信任与防误伤（用户量破百前必须完成）

- **恢复误判 → 永久白名单**：每条黄框配"误判"按钮，点击后该评论恢复正常显示，账号写入本地白名单永不打扰；
- **白名单管理面板**：本地白名单/黑名单的查看、增删、导入导出；
- **保守阈值分档**：识别结果分两档——高置信（多特征命中）才标黄；中置信默认只做淡色提示或折叠，可在设置中关闭；
- **字符归一化指纹**：全角转半角、零宽字符/特殊符号剥离后再匹配，对抗字符伪装变种；
- **公共规则库热更新**：词库 JSON 托管在 GitHub（Raw/jsDelivr 分发），带版本号，插件定期拉取；用户可一键查看当前规则全文（公开透明）；
- **操作日志**：本地记录每次拉黑的时间、账号、命中的规则，可回溯可审计。

### P2 · 体验与游戏化（"打野"心智）

把清理从"厌烦的劳动"重塑成"解压的打野"，相关内容互动率暴涨 300%+。推妖镜的"照妖"叙事天然适合这套玩法。

- **战报统计**：本地累计"照妖数 / 收妖数"，popup 面板展示；
- **清理动效**：一键清理时黄框逐个碎裂消失（纯 CSS，零成本解压感）；
- **称号系统**：本地成就（照妖学徒 → 驱魔师 → 妖镜宗师），纯本地计算，不建后端；
- **模式切换**：宽松 / 标准 / 严格三档灵敏度；
- **深色模式适配 + 中英双语**。

### P3 · 进阶与商业化探索（不急于交付）

- **本地模型识别**：接入浏览器本地模型（Chrome 内置 Gemini Nano / Transformers.js），识别高维度垃圾（软文引流、无意义抬杠），对应用户增值点；
- **跨设备规则同步**：走用户自带的 WebDAV / Gist，插件本身不建账号体系；
- **社区黑名单共享**：脱敏聚合用户拉黑记录形成社区黑名单，随公共规则库分发；
- **企业版评论区守卫**（B 端，暂缓）：品牌官方号的 24h 评论区巡逻，月付；

---

## 4. 识别引擎设计

**零额外请求原则：识别阶段纯本地 DOM 分析，不发任何网络请求。** 只有用户点击拉黑时才发起原生拉黑调用。这同时是风控保护和隐私卖点。

### 特征分层与评分制

| 层 | 特征 | DOM 可得性 | 置信度 |
|----|------|-----------|--------|
| 文本规则 | 引流黑话词库（"看我主页""福利""私信""上门"类）、钓鱼链接模式、全角/异形字符伪装的网址 | 评论正文 | 高 |
| 结构特征 | 内容与原帖无关、复读/截取原帖句子、纯链接或纯图片无文字 | 评论上下文 | 中 |
| 账号特征 | 昵称含联系方式模式、默认头像、昵称特殊符号堆砌 | 昵称/头像 | 低（单独不触发） |

- 评分制：每条命中特征加分，总分 ≥ 高置信阈值才标黄；中置信区间只做可选的淡色提示；
- 白名单（登录用户 / 本地 / 社区）在评分前短路，永不错杀；
- **快速对抗机制**：发现新变种当天走"抓 10 个真实样本 → 本地脚本验证规则 → 发版"流程，规则库支持热更新意味着大部分对抗不用等商店审核。

---

## 5. 原生拉黑与防风控

### 调用方式

复用用户在 x.com 的登录会话（cookie + csrf token），调用 X 网页端拉黑时使用的同款原生接口。请求从用户本地浏览器发出，用用户自己的身份——插件是"代为点按钮"，不是独立爬虫。

### 防风控硬规则（0.x 版就位，不是以后再说）

1. **绝不并发**：拉黑队列串行执行，一批大热帖几十个目标时，宁可在后台慢条斯理跑半分钟；
2. **随机拟人延迟**：相邻请求间隔随机 800ms–2s；
3. **429 熔断**：捕获到限流响应立即全线静默，绝不盲目重试，UI 提示"触发平台限流，队列已暂停"，冷却后（如 10 分钟）自动恢复或用户手动恢复；
4. **每日拉黑上限**：默认上限（如 50/天，可调），防止手滑在热帖里一键清掉几百个触发风控；
5. **幂等标记**：已拉黑账号本地打标，同一账号永不重复发请求。

### 手动确认的价值

不做成静默全自动，除了风控，还有心理账：黄框标出来、用户亲手按下清理、看着屏幕瞬间清爽——掌控感和解压感本身就是产品价值。

---

## 6. 防误伤体系（三层白名单 + 申诉出口)

1. **登录用户**：硬编码白名单，任何规则不生效；
2. **本地白名单**：用户点"误判"即永久豁免，导出可迁移；
3. **社区白名单**：随公共规则库分发，保护已知的正常账号（如昵称带特殊符号的知名博主）；
4. **显眼的恢复入口**：每条黄框都有"误判"按钮——申诉出口的可见性等于用户的敢用程度。

治理类工具的铁律：**宁可漏掉两个，绝不大规模误杀。**

---

## 7. 技术架构

```
┌─ Chrome Extension (Manifest V3)
│   ├─ content_script.js    DOM 监听、特征匹配（纯本地）、UI 注入（黄框/按钮/徽章）
│   ├─ service_worker.js    拉黑队列调度、限流熔断、规则库定时拉取
│   ├─ rules engine         评分制匹配器 + 字符归一化（纯函数，可单测）
│   └─ storage              chrome.storage.local：白名单/已拉黑标记/统计/日志/规则缓存
├─ 规则分发                  GitHub 仓库 JSON → jsDelivr/Raw，带版本号（后续可加签名防投毒）
├─ 开源仓库                  GitHub（MIT 或 GPL，全文可审计）
└─ 官网                     GitHub Pages 静态页（规则公示 + 隐私声明 + 安装引导），零成本
```

- **权限最小化**：仅 `storage` + `x.com` 域名 host 权限，无 tabs、无历史、无远端代码；权限清单本身就是信任文案；
- **隐私承诺**：不收集、不上报任何用户数据，无远端 telemetry——写进商店描述和官网第一屏；
- Firefox 移植预留：核心逻辑写成浏览器无关的纯模块。

---

## 8. 信任建设清单（上架即备齐）

这类"能代替你操作账号"的工具，信任成本决定转化天花板。

- [ ] 源码完整开源，README 写清架构与数据流；
- [ ] 隐私政策页：不收集任何数据的一句话说清楚；
- [ ] 权限清单逐条解释为什么需要；
- [ ] 防风控机制（串行/延迟/熔断/上限）在 README 明文公示；
- [ ] 规则词库全文公开可查；
- [ ] 商店审核材料：权限用途说明 + 演示截图/视频。

---

## 9. 里程碑

| 阶段 | 内容 | 出口标准 |
|------|------|---------|
| M1 | P0 MVP | 自己在真实信息流用两天，"看到黄框点一下"足够爽 |
| M2 | P1 防误伤 + 规则热更新 | 小范围（朋友/群）邀请测试，零误杀反馈 |
| M3 | P2 游戏化 + 双语 + 深色模式 | 上架 Chrome 商店，GitHub 开源发布 |
| M4 | 官网 + 规则公示 + 隐私页 | 正式推广，开始社区运营 |

---

## 10. 风险提示（务必知悉）

- **类似功能插件福滤娃已于 2026-09-14（复盘发布当天）主动收摊**：仓库转私、官网关闭。作者澄清"没有被封，是主动下架"，但原因表述为"个人原因以及其他一些现实考量"。这说明这个赛道在需求真实的同时，也存在现实压力（平台关系、灰产骚扰、个人隐私暴露等）。建议：推广时考虑身份隔离策略，对可能到来的骚扰有心理预期，并提前想好自己的退出条件。
- **平台 ToS 风险**：调用原生拉黑接口属于"用户代操作"灰色地带，频率控制和拟人化设计（第 5 节）是生命线，任何"提速"需求都应被否决。
- **对抗持久战**：黑产变种是常态（全角字符、复读原帖等都是真实战例），规则热更新 + 当天响应机制是护城河；规则公开会引导黑产升级拟真成本——这本身就是胜利（抬高其作恶成本至不划算）。
- **误杀舆情风险**：一次大规模误杀截图的传播力远超一百条好评，防误伤体系的优先级永远高于识别率。

---

## 附：传播备忘（福滤娃增长数据验证，推广期再用）

- **痛点必须"足够恶心"才有人装**：福滤娃帖子的收藏/点赞比高达 0.74（大盘仅 0.1–0.2），用户在囤工具——需求真实性已被验证；
- **荒诞真实截图 > 精剪视频**：机器人跑到插件宣传帖下主动送人头的截图帖，点击率 4.45%，是打野视频（0.24%）的 18.5 倍，单帖贡献全项目 43% 的点击。视频赚声量，话题性图文赚转化；
- **日志式功能更新帖是最差素材**（1.4 万曝光仅 112 点击）：版本更新要包装成战报或故事来发；
- **点击→安装转化约 7.5–8%**（行业普遍 2–3%）：商店页首屏截图与评论是临门一脚，值得专门打磨。

---

## 11. 一句话总结

> 推妖镜不发明新需求，它只是把天天扎在每个 X 用户手里的那根刺拔掉：**照妖镜一照，妖怪现形；你亲手一点，世界清静。**
